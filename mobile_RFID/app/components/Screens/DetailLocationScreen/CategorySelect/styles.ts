import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  selectView: {
    borderWidth: 1,
    borderStyle: "solid",
    borderColor: "gray",
    borderRadius: 10,
    overflow: "hidden",
  },
})

export default styles
