import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    width: "100%",
    marginBottom: 20,
    flexDirection: "row",
    gap: 10,
    alignItems: "center",
  },
})

export default styles
